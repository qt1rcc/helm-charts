apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.fullnameOverride }}-frontend
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.fullnameOverride }}-frontend
  template:
    metadata:
      labels:
        app: {{ .Values.fullnameOverride }}-frontend
    spec:
      containers:
        - name: app
          image: {{ .Values.sample-node-app-frontend.image.repository }}:{{ .Values.sample-node-app-frontend.image.tag }}
          ports:
            - containerPort: {{ .Values.sample-node-app-frontend.image.port }}
          imagePullPolicy: IfNotPresent
      imagePullSecrets:
      - name: regcred